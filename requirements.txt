# ============================================================
# 《无尽的拉格朗日》AI 战术推演中心 — Python 依赖清单
# ============================================================
# 安装方式：pip install -r requirements.txt
# 使用轻量级 TF-IDF + FAISS 方案，无需下载大模型文件

# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.30.0

# 数据校验
pydantic==2.9.0

# 环境变量加载
python-dotenv==1.0.1

# JWT 认证
python-jose[cryptography]==3.3.0

# 密码哈希（bcrypt）
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# HTTP 客户端（调用 DeepSeek API）
httpx==0.27.0

# 向量检索（FAISS — Windows 预编译 wheel，无需 C++ 编译工具）
faiss-cpu==1.8.0

# TF-IDF 文本向量化（轻量，替代大模型嵌入）
scikit-learn>=1.3.0

# 中文分词（jieba 分词器，提升中文检索精度）
jieba>=0.42.0

# 异步 SQLite
aiosqlite==0.20.0

# 文件上传处理
python-multipart==0.0.9

# numpy（FAISS 依赖）
numpy>=1.24.0,<2.0.0
